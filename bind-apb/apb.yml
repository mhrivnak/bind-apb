version: 0.1
name: bind-apb
description: Bind9 running on CentOS 7
bindable: False
async: optional
metadata:
  displayName: bind (APB)
plans:
  - name: default
    description: This default plan deploys bind-apb
    free: True
    metadata: {}
    parameters:
      - name: bind_forwarders
        title: Forwarders
        type: string
        default: '8.8.8.8;'

