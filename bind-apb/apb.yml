version: 1.0
name: bind-apb
description: BIND9 running on CentOS 7
bindable: False
async: optional
metadata:
  displayName: BIND (APB)
plans:
  - name: default
    description: Deploys BIND9 as a caching DNS server.
    free: True
    metadata: {}
    parameters:
      - name: bind_forwarders
        title: Forwarders
        type: string
        default: '8.8.8.8;'

